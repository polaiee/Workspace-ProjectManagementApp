<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.TrelloClone" parent="Theme.MaterialComponents.Light.NoActionBar">
        Primary brand color.
        <item name="colorPrimary">@color/colorAccent</item>
        <item name="colorPrimaryVariant">@color/white</item>
        <item name="colorOnPrimary">@color/colorPrimary</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/white</item>
        <item name="colorSecondaryVariant">@color/white</item>
        <item name="colorOnSecondary">@color/white</item>
        <item name="cardViewStyle">@style/MyStyle.Cardview.white</item>
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />

    <style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />

    <style name="Theme.TrelloClone.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
    </style>

    <!-- CardView -->
    <style name="MyStyle.Cardview.white" parent="CardView">
        <item name="cardBackgroundColor">@color/white</item>
    </style>


    <color name="black">#000</color>
    <color name="white">#FFFFFFFF</color>
    <color name="colorAccent">#0C90F1</color>

</resources>